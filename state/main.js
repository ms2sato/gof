// Generated by CoffeeScript 1.6.3
(function() {
  var PauseStatus, PlayingStatus, StopStatus, StopWatch, StopWatchView;

  StopStatus = (function() {
    function StopStatus(app, view) {
      this.app = app;
      this.view = view;
    }

    StopStatus.prototype.onBeforeChangeStatus = function() {
      this.view.decoratePlayButton(0);
      return this.view.decorateDisabledLapButton(1);
    };

    StopStatus.prototype.clickedMainButton = function() {
      return this.app.start();
    };

    StopStatus.prototype.clickedSubButton = function() {};

    return StopStatus;

  })();

  PauseStatus = (function() {
    function PauseStatus(app, view) {
      this.app = app;
      this.view = view;
    }

    PauseStatus.prototype.onBeforeChangeStatus = function() {
      this.view.decoratePlayButton(0);
      return this.view.decorateResetButton(1);
    };

    PauseStatus.prototype.clickedMainButton = function() {
      return this.app.start();
    };

    PauseStatus.prototype.clickedSubButton = function() {
      return this.app.reset();
    };

    return PauseStatus;

  })();

  PlayingStatus = (function() {
    function PlayingStatus(app, view) {
      this.app = app;
      this.view = view;
    }

    PlayingStatus.prototype.onBeforeChangeStatus = function() {
      this.view.decorateStopButton(0);
      return this.view.decorateLapButton(1);
    };

    PlayingStatus.prototype.clickedMainButton = function() {
      return this.app.pause();
    };

    PlayingStatus.prototype.clickedSubButton = function() {
      return this.app.lap();
    };

    return PlayingStatus;

  })();

  StopWatch = (function() {
    function StopWatch() {
      this.counter = 0;
      this.lastLap = 0;
    }

    StopWatch.prototype.setup = function(el) {
      this.view = new StopWatchView(this, $(el));
      this.statuses = {
        stop: new StopStatus(this, this.view),
        pause: new PauseStatus(this, this.view),
        playing: new PlayingStatus(this, this.view)
      };
      return this.changeStatus(this.statuses.stop);
    };

    StopWatch.prototype.clickedMainButton = function() {
      return this.status.clickedMainButton();
    };

    StopWatch.prototype.clickedSubButton = function() {
      return this.status.clickedSubButton();
    };

    StopWatch.prototype.start = function() {
      var _this = this;
      this.changeStatus(this.statuses.playing);
      return this.timer = setInterval(function() {
        _this.counter++;
        return _this.view.updateDisplay();
      }, 100);
    };

    StopWatch.prototype.pause = function() {
      clearInterval(this.timer);
      return this.changeStatus(this.statuses.pause);
    };

    StopWatch.prototype.reset = function() {
      this.counter = 0;
      this.view.reset();
      return this.changeStatus(this.statuses.stop);
    };

    StopWatch.prototype.lap = function() {
      var lap;
      lap = this.counter - this.lastLap;
      this.lastLap = this.counter;
      return this.view.addLap(lap);
    };

    StopWatch.prototype.changeStatus = function(next) {
      this.status = next;
      return this.status.onBeforeChangeStatus();
    };

    return StopWatch;

  })();

  StopWatchView = (function() {
    function StopWatchView(app, el) {
      var _this = this;
      this.app = app;
      this.$el = $(el);
      this.$('.buttons button.main').click(function(event) {
        return _this.app.clickedMainButton();
      });
      this.$('.buttons button.sub').click(function(event) {
        return _this.app.clickedSubButton();
      });
    }

    StopWatchView.prototype.$ = function(options) {
      return this.$el.find(options);
    };

    StopWatchView.prototype.getButton = function(index) {
      return this.$(".buttons li:eq(" + index + ") button");
    };

    StopWatchView.prototype.decoratePlayButton = function(index) {
      return this.getButton(index).text('play').removeAttr('disabled');
    };

    StopWatchView.prototype.decorateStopButton = function(index) {
      return this.getButton(index).text('stop').removeAttr('disabled');
    };

    StopWatchView.prototype.decorateResetButton = function(index) {
      return this.getButton(index).text('reset').removeAttr('disabled');
    };

    StopWatchView.prototype.decorateLapButton = function(index) {
      return this.getButton(index).text('lap').removeAttr('disabled');
    };

    StopWatchView.prototype.decorateDisabledLapButton = function(index) {
      return this.getButton(index).text('lap').attr('disabled', true);
    };

    StopWatchView.prototype.updateDisplay = function() {
      return this.$('.display').text(this.app.counter);
    };

    StopWatchView.prototype.addLap = function(lap) {
      return $("<li>" + lap + "</li>").prependTo(this.$('.laps'));
    };

    StopWatchView.prototype.clearLaps = function() {
      return this.$('.laps').empty();
    };

    StopWatchView.prototype.reset = function() {
      this.updateDisplay();
      return this.clearLaps();
    };

    return StopWatchView;

  })();

  $(function() {
    var stopWatch;
    stopWatch = new StopWatch();
    return stopWatch.setup($('.stopwatch'));
  });

}).call(this);

/*
//@ sourceMappingURL=main.map
*/
